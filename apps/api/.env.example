PORT=3000
APP_URL=http://localhost:5173

# Local file DB (default). For Turso cloud: libsql://<db>.turso.io
DB_URL=file:./movie-talkin.db
# DB_AUTH_TOKEN=   # only needed for Turso cloud

CORS_ORIGIN=http://localhost:5173
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
SESSION_SECRET=

TMDB_API_KEY=

# Seed script (pnpm db:seed)
SEED_HOST_NAME=
SEED_HOST_EMAIL=   # optional
SEED_GROUP_NAME=
